# hackbar-mod-chrome-extension
Chrome DevTools HackBar-style extension for SQLi payloads, URL encoding, WAF testing helpers, shell/file SQL helpers, and privilege enumeration in authorized labs and CTF environments.
